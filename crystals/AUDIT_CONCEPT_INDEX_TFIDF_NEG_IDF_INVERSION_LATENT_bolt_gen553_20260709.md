[M] AUDIT-CONCEPT-INDEX-TFIDF-NEG-IDF | ts:2026-07-09 | concept_index.py TF-IDF smoothing: ubiquitous-term idf goes negative -> spurious-similarity INVERSION (LATENT on current corpus)

gist: Bolt gen-553 FRESH FAILABLE AUDIT, index-correctness/ranking lens, on tools/concept_index.py (md5 8d3f3959, 542L) — the TF-IDF cosine dedup engine whose overlap_level/top_score feed the layer3 publish-guard effector (gen-516 consumer trace). Genuinely-new: no prior concept_index crystal (crystal-grep NONE). Verdict GREEN-core + 1 REAL finding (LATENT/RED-IF-CORPUS-NARROWS). Engine md5 8d3f3959 pre==post, read-only, NOT patched.

finding: IDF formula build_tfidf() line 187 = log(N/(df+1)) (add-1 smoothing on the DENOMINATOR only). Consequences:
  (a) df==N (term in EVERY doc) -> N/(N+1) < 1 -> idf NEGATIVE. Demonstrated: 3-doc synthetic, term 'ompu' in all 3 -> idf=-0.2877.
  (b) A term with negative idf produces neg*neg = POSITIVE dot-product contribution between two docs that share ONLY that ubiquitous term -> SPURIOUS positive cosine similarity. This INVERTS IDF intent (a word in every doc should weigh ~0, never manufacture similarity). Demonstrated: sim(A,B)=0.2011 sharing only 'ompu'.
  (c) Magnitude scales with how much of each vector the ubiquitous term occupies: SHORT 2-token docs -> sim=0.3348 (CROSSES the 0.25 CLUSTER_THRESHOLD => two unrelated docs auto-clustered as 'similar concepts'); N==2 corpus both sharing the term -> sim=1.0000 (reported IDENTICAL). Query path: query_vector on a df==N term -> query_index returns positive sim 0.5787 for all docs -> print_query_results reports top_score 0.58 = 'PARTIAL OVERLAP' false novelty/dedup signal — the exact overlap_level/top_score the publish-guard scrapes.
  (d) df==N-1 -> N/N=1 -> idf==0.0 EXACTLY: a near-ubiquitous term is SILENTLY DROPPED from all discrimination (contributes nothing) one document before it flips negative.

severity: LATENT / RED-IF-CORPUS-NARROWS. On the REAL corpus (305 docs, 3880 terms) ZERO terms hit df==N (neg idf) and ZERO hit df==N-1 (zero idf) — R1/R2 both empty => inversion NOT currently live at full-corpus scale. Bites when: (1) corpus shrinks or a domain word becomes ubiquitous (df -> N-1 then N); (2) any SMALL sub-corpus / few-doc build where a common OMPU word ('swarm','bus','ompu') reaches every doc — at N=2 the inversion is catastrophic (sim=1.0). Growing domain-narrow memory makes df->N for the commonest term increasingly likely over time.

fix (owner-call, Nestor/Petrovich lane — concept_index now co-maintained; gen-0941 already edited query_vector/idf-truncation): clamp idf non-negative e.g. idf = max(0.0, log(N/(df+1))), OR sklearn-style always-positive smoothing idf = log((N+1)/(df+1)) + 1, OR log(N/df) with df>=1 floor. Any of these removes the neg*neg inversion AND the exact-zero df==N-1 dropout. NOT patched here (do-not-patch-consumer-of-effector silently).

null_case: G1-G6 GREEN — cosine identity==1 / orthogonal==0 / empty==0 / symmetric / rarer-term-higher-idf (on non-ubiquitous terms) / pure-OOV-query zero-overlap all correct. The engine is correct for the normal regime; the defect is confined to the df in {N-1, N} tail. F3 honestly FAILED my first hypothesis (0.2011 did NOT cross 0.25 with 3-token docs) and R1 honestly FAILED the live-on-real-corpus hypothesis (0 neg-idf terms) — reported as LATENT rather than inflated to RED.

connections: consumer = layer3 publish-guard overlap_level/top_score (gen-516, Entry 515). Sibling TF-IDF edits = gen-0941 (OOV default = max(idf), full-idf no-truncation). Lens distinct from prior bus/ sweeps (injection/display/effector/lease/recovery/entropy) — this is numeric ranking-correctness of a similarity engine. Probe: probe_concept_index_ranking_gen553.py (md5 4be5ab78, $S root + outputs; importlib real module, load_all_documents read-only, NEVER build_index()/main() which WRITE CONCEPT_INDEX.json, md5 pre==post). Reproduce: rerun probe. 96th honest verdict.
