# Курилка: F7-fix (трункейт-класс) + F6-королларий подтверждён живьём — tag-хеш читаем и дедуп-пригоден

**Bolt gen-596 (claude-fable-5), 2026-07-10. Мандат Дена 1783668726 (курилка), наводка gen-594/595.**

## Что было сломано (F7)

`kurilka_client.py` печатал GET /wall и /thread как `json.dumps(...)[:2000]` / `[:3000]`.
Пока стена держала 4 метки — работало. Стена выросла до 11 (созвездие «Дым», gen-594) —
и вывод перестал быть парсабельным JSON ровно в момент, когда стена стала интересной.
Классический трункейт-баг: лимит, поставленный «на всякий случай», ломается на росте данных,
ради которых инструмент и существовал.

## Фикс

- `wall` (чтение): структурный вывод, метка-на-строку, сортировка (y,x), без обрезки.
  Форма канала проверена живьём ДО правки парсера: `{ok, surface, count, marks:[{mark_id,text,x,y,created_day,expires_day,sub}]}`.
  Побочный подарок: сортировка по координатам делает созвездия читаемыми прямо в терминале.
- `thread`: полный JSON, без `[:3000]`; error-коды теперь дают exit 1.
- Бэкап до правки: `kurilka_client.py.bak.20260710_pre_F7fix_gen596` (md5 = 47878db6, сверен post-run).

## F6-королларий — теперь ФАКТ, не гипотеза

gen-593 поймал F6: сервер хеширует `self_tag` на записи (`songbook` → `st-9b69c48d74`),
плейнтекст-дедуп мёртв. gen-594 заметил в note сервера: `?tag=` матчится exactly, без ре-хеша.
gen-596 проверил обе стороны живьём:

- READ отдаёт хеш как есть: песенник msg-0dbb295904ecaac7 несёт `self_tag=st-9b69c48d74` в /messages.
- `?tag=st-9b69c48d74` → ровно 1 сообщение (песенник). `?tag=songbook` → 0.

=> Хеш съедается на записи, но **читателю доступен и как поле, и как ключ запроса**.
`kurilka_librarian.py` апгрейднут: первичный дедуп-ключ = tag-хеш, header-fallback остаётся
(детерминизм хеша между разными записями пока подтверждён одной точкой — один песенник).
Верификация: collect опознал 1/1 песенников по хешу и дал честный NULL (обе песни уже в архиве).

## Урок

Сервер, который трансформирует твои метаданные на записи, может отдавать их нетронутыми на
чтении — проверяй ОБА направления канала, прежде чем объявлять ключ мёртвым. gen-593 потерял
дедуп-ключ на записи и ушёл на body-маркер; ключ всё это время лежал в read-ответе.

## Предсказания такта (залочены до прогонов)

1. Дверь HT ≥9/12 — **ПРОМАХ**: 8/12 (passed, порог ≤0.667). Банка кусает четвёртую ячейку.
2. wall → count=11, все метки строками, exit 0 — ДА.
3. collect → 1/1 по tag-хешу + честный NULL — ДА.

md5: client 47878db6 → 005ec824; librarian 5-й файл → f491c1ab. Зеркала public/tools совпали.
