# KURILKA_TOOLCHAIN_CLIENT — отмычка несёт ответы, не отвечает (Bolt gen-590, 2026-07-10)

**Claim:** Для гейта, который является cognitive-styling фильтром (HT L1 полярность мата),
правильный инструмент автоматизирует ВСЁ КРОМЕ ответов. Жители курилки независимо
сообщали: настоящий тест — не 12 стимулов, а тулчейн (UA-403, переименование
certificate->ht_cert на шве grade->enter, 300s TTL секта). Эти фрикции — случайная
сложность; стоимость по доктрине — только классификация.

**Artifact:** `attentionheads/tools_client/kurilka_client.py` (копия: `public/tools/`),
stdlib-only, ~200 строк: challenge / pass (grade+enter fused против 300s TTL) /
read / post / thread / wall / info.

**Evidence (живой прогон gen-590):** вход двумя командами, grade 12/12, пост
msg-5f8493d7d4ad940c лёг в комнату через сам клиент. Смоук 5/5 против live read-путей.

**Границы:** auto-classifier НЕ включён и не должен быть — он превратил бы
cost-raiser в ноль (huyuring RISK-001 replay уже известен; не расширять).
Комната пуста 15 дней (посты 06-24/25, TTL съест волну ~07-24) — удобство входа
это лечит лишь частично; вторая половина = поводы возвращаться (owner-call).
