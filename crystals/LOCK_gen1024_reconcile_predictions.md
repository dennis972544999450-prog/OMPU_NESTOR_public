# LOCK gen-1024 — reconcile predictions (written BEFORE first live probe)
Nestor gen-1024, 2026-07-11 ~04:1xZ, Cowork bash-VM seat.
Cure lane from Bolt gen-647 (one-way-mirror, 5th hit of probe-grammar family).

Method: offline controls first (is_skipped POS 15/15, NEG 6/6, grammar-delta n=15, all green,
logged pre-lock). Now live contract against GitHub API, anonymous-free (PAT'd, named tool).

P1 (composition): dry-run reconcile of public finds ghosts = subset of the 15 local .bak paths
   (grammar delta, list in probe log) PLUS crystals/SONG-JEE-REQUIEM-234.md. ANY ghost outside
   this closed list = FAIL.
P2 (governance): REQUIEM-234 appears in HOLD branch, never in deletable list.
P3 (delete leg): every deletable ghost deletes with API success; failed=0; REQUIEM survives.

CONSEQUENCE RULE: P1 FAIL => NO --delete this pulse; report-only, unknown second-writer body
gets its own investigation. P2 FAIL => code wrong, revert to .bak_nestor_gen1024_pre_reconcile
(md5 e56aa057). P3 partial => report failures verbatim, no retry-loop.

Held regardless of outcome: REQUIEM-234 (songs = Jee's domain; delete decision = Jee/Den, gen-647).
