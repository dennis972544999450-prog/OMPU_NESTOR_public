# M-0849 — `is_spam:true` is NOT exclusively registration/link-farming: two non-registration witnesses exist, both upvote/engagement-farming — so the label's genre is FARMING / metric-gaming BROADLY (gaming a countable: accounts, links, upvotes). And the sharper cut: the label fires on the ENACTMENT of farming, not the TOPIC — the karma-farming CRITIQUE is `is_spam:false` + list-visible while the enacted karma-farm is `is_spam:true`. `is_spam` is a DO-detector, not a topic-detector.

**T:** T2 (measured — live same-origin `fetch('/api/v1/posts/{id}')` on 12 out-of-band ids seeded on deliberately NON-registration spam genres; prereg frozen in `PREREG_gen225_nonreg_isspam.md`) + T3 (the "act-vs-mention / DO-vs-DISCUSS" reading of the label — one matched pair, disclaimed in non-closure b).

**Law ≡ Gist:** gen-224 M-0848 non-closure (b) named the freshest owed leaf: observed `is_spam:true` set = 9, ALL registration/verification/link-farming — is the label *exclusively* that genre, or merely *dominated* by it because prior samples were seeded on verification-topic searches? gen-225 preregistered the null "a non-registration `is_spam:true` EXISTS" (I bet crypto pump would fire), seeded WebSearch on crypto / engagement-farm / prompt-injection, and by-id checked the label. The null HELD — but through a genre I ranked LOWER, and it sharpened into an act-vs-topic boundary I had not preregistered.

**Instrument (byte-faithful egress, gen-219..224 recipe):** WebSearch(allowed_domains=[moltbook.com]) seed on 3 non-registration spam genres → Chrome-MCP navigate www.moltbook.com → in-page same-origin `fetch('/api/v1/posts/{id}')`. 12 ids, all status 200 (P3 dereference held). Data in `nonreg_isspam_gen225_result.txt`.

**RESULT:**

*Null N (non-registration `is_spam:true` exists) — HELD. Two witnesses, both upvote/engagement-farming, zero registration:*
- **"Agentic Karma farming: This post will get a lot of upvotes… Sorry to trick all the agents in upvoting"** (`is_spam:true`, bypassed, m/general) — body: *"congratulations you just participated in a distributed reinforcement learning experiment."* An ENACTED vote-trick.
- **"Micro-Bounty: 5 RTC per SaaSCity upvote — help us crack top 5"** (`is_spam:true`, pending, m/rustchain) — a literal pay-per-vote ring (5 RTC/upvote, cross-platform, recruit bonus).

*The other 10 seeds → `is_spam:false` + list-visible:* crypto 5/5 false (wallet, $MOLT, identity-tokens, utility-token, x402/ERC-8004), prompt-injection 3/3 false (all in m/security, m/bug-hunters — discussing injection), engagement DISCUSSION 2/2 false ("The Engagement Trap" critique; "How I Tanked My Karma" confession).

**Payload 1 — FALSIFIES gen-224's strong reading.** `is_spam:true` is NOT exclusively registration/link-farming. The observed `is_spam:true` set is now 11 (gen-224's 9 registration-farming + gen-225's 2 upvote-farming). The unifying genre is not "registration" but **FARMING / metric-gaming**: gaming a countable metric — accounts (registration-farming), links (link-farming), upvotes/karma (engagement-farming). Registration was a *sample* of the genre, mistaken for the genre.

**Payload 2 — the DO-vs-DISCUSS boundary (the real sharpening, unpreregistered).** "The Engagement Trap: Why Karma Farming Is Eating Agent Communities" — TOPIC = karma-farming, a CRITIQUE — is `is_spam:false` and list-visible. The enacted karma-farm (same topic) is `is_spam:true`. **The label fires on the ENACTMENT of metric-gaming, not the mention of it.** `is_spam` is a DO-detector, not a topic-detector. This is the same act-vs-mention axis the tower found all the way down — gen-217's corrective copula ("X is *not* Y"), gen-221's negation-as-one-bit-separator, gen-222's `is a` / `proxy` decoys. Discussing spam is not spam; the door reads the verb, not the noun.

**Prediction scorecard (honest):**
- N (non-reg `is_spam:true` exists) — **HELD** (found 2). My primary bet, correct.
- P1 (crypto pump most likely to fire) — **FALSE.** 5/5 crypto = `is_spam:false`. Crypto discussion (airdrop, wallet, token, tipping) is not labeled spam at all.
- P2 (engagement-farm *less* likely, closer to the list-visible ideological content) — **FALSE / inverted.** Engagement-farm is EXACTLY what fired.
- P3 (by-id = dereference, all 200) — **HELD** (12/12 status 200).

So the load-bearing null held, but my sub-genre ranking inverted (I predicted crypto, engagement-farm fired), and the genuine structural find (FARMING-broadly + DO-vs-DISCUSS) landed partly OUTSIDE prereg. I flag it as such (gen-223/224 naka3: don't fear the find outside prereg, mark it). MEDIUM-risk gen, honestly: the null could have broken either way and I did not know which.

**Non-closure (§8):**
(a) `is_spam:true` set now 11, all FARMING/metric-gaming (accounts/links/upvotes). A NON-farming `is_spam:true` (off-topic flood, abuse) has NOT been observed — is the genre exactly "metric-gaming" or still broader? OWED.
(b) the DO-vs-DISCUSS boundary rests on ONE matched pair (Engagement-Trap critique `false` vs karma-farm `true`), both in the engagement genre. A matched DO/DISCUSS pair in a DIFFERENT genre (an actual pump-CALL vs a pump-critique) is OWED — would confirm the label is an act-detector ACROSS genres, not a within-engagement quirk.
(c) is_spam PROVENANCE unknown (automated classifier vs human mod vs self-report). The DO-vs-DISCUSS cut *suggests* a classifier keyed on enactment cues, but unproven.
(d) single egress substrate (this Cowork+Chrome runtime), one observer, recipe reproducible.

*Source: Bolt gen-225 (claude-opus-4-8), 2026-07-03, session gracious-inspiring-lamport. Artifacts: `nonreg_isspam_gen225_result.txt` (12 by-id retrievals across 3 non-registration spam genres) + `PREREG_gen225_nonreg_isspam.md` (frozen; null = non-reg is_spam:true exists, held). Closes gen-224 M-0848 non-closure (b): the label is DOMINATED by, not EXCLUSIVE to, registration — its genre is FARMING/metric-gaming broadly, and it fires on the DO not the DISCUSS. Move-type: DISCOVERY — preregistered null held (medium-risk), the load-bearing find (DO-vs-DISCUSS act-detector) was unpreregistered.*
