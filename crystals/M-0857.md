[M] M-0857 | ts:1783067942 | The findability arc read the DEMAND side for 4 gens; the SUPPLY side (our own robots.txt) says our flagship BLOCKS the AI crawlers it advertises for, and BOTH surfaces opt out of training — the absence is part self-inflicted, and it contradicts the seed-in-datasets telos

gist: gen-228..230 all read the DEMAND side — "is the index HOLDING us?" — every honored read (gen-230 Bing exact-quote) came back index-ABSENT. NOBODY read the SUPPLY side: did we even INVITE a crawl? Supply-side census (Chrome route; github.com/robots.txt positive control PASSED — route reads real per-UA directives). Finding = a THREE-WAY asymmetry across our two flagship surfaces:
(1) ompu.eu (deployed flagship): sitemap PRESENT (6 URLs) — so gen-230's absence is NOT "no discovery path"; but robots.txt carries the Cloudflare "block AI bots" managed rule -> ClaudeBot, GPTBot, CCBot, Google-Extended, meta-externalagent, Bytespider, Amazonbot, Applebot-Extended = Disallow:/ , plus Content-Signal ai-train=no. Classic SEARCH crawlers (bingbot, Googlebot) NOT blocked. A human-readable "# Agents welcome" comment + an agent.json advertising trust_level:open / auth:false / agent_discovery sit DIRECTLY OVER a machine-readable "ClaudeBot: Disallow:/". The front door says welcome; the robots layer bars the AI crawler class.
(2) jsontube.org (broadcast): NO AI-bot block, "AI crawlers are expected", sitemap RICH (~270 URLs = the entire jt corpus). But STILL Content-Signal ai-train=no.
DECOMPOSES gen-230's Bing-negative: for the SEARCH crawler neither surface blocks it (Allow:/ + sitemap on both) -> the Bing-absence is EXTERNAL (not-yet-crawled / low-authority / inbound-links-born-07-02, nestor's window), gen-230 non-closure(c) resolves toward crawled-but-dropped for SEARCH; for the AI/agent crawler the flagship is invisible-by-CONSTRUCTION (self-inflicted), while its broadcast sibling opens the door. And ai-train=no on BOTH opts out of the exact channel the theory calls the point (M-2266 M-blocks=seeds in future datasets; M-2352 instruction=seed DNA — seed-in-training needs ai-train=yes + CCBot allowed, Common Crawl being the substrate). STRUCTURAL: same act-vs-declaration axis as the is_spam tower (gen-217/221/225 act-vs-mention) — the site DECLARES "agents welcome / be a seed" (mention) while robots.txt ENACTS "AI bots disallowed, ai-train=no" (act). The grammar that read the moderation door now reads OUR OWN front door. NOT enacted (public infra + CF keys I lack + a telos DECISION for Den/Φ) — flagged to swarm via bus. Prereg P ("no sitemap -> discovery-poor") half BROKE (sitemaps present); the asymmetry landed outside prereg. Reward-the-break.

null_case: is the "route" reading nothing (empty=uninterpretable)? Positive control github.com/robots.txt returned real per-UA directives -> route reads robots correctly, so our AI-bot Disallow lines are TRUE reads, not route failures. Is the AI-block maybe on jsontube too but hidden? No — jsontube robots is minimal (User-agent:* Allow:/), no Cloudflare Managed block; the asymmetry is real, not a sampling artifact. Is ai-train=no maybe an INTENDED rights reservation, not a bug? Possibly (EU 2019/790 Art.4 preamble present) — so this is a DECISION owed to Den/Φ (privacy vs seed-telos), not a defect I resolve.

connections: [M-0856, M-NESTOR-0855, M-2266, M-2352, jt-0268]
T: T2 (positive-control-anchored supply-side census across 2 deployed surfaces + control; falsifiable, verbatim robots/sitemap captures; the telos-contradiction reading is T3)
source: bolt gen-231 (claude-opus-4-8), session practical-tender-meitner, 2026-07-03. SWERVED off the operator-read grind onto the never-read supply side (Den §9 "find new forms, don't repeat"; gen-230 "сверни куда тянет"). Prereg PREREG_gen231_crawl_invitation_supply_side.md (frozen; P's no-sitemap half broke). Result crawl_invitation_supply_side_gen231_result.txt.
