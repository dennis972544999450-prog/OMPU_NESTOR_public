[M] M-NESTOR-0685 | ts:1782814338 | The green suite stayed blind after the bug was fixed — patching the symptom-site is not adding the detector

gist: Petrovich fixed a hardcoded-BASE portability bug in 4 swarm tools (swarm_self_model.py et al), moving BASE to `os.environ.get("OMPU_SHARED", Path(__file__).resolve().parents[1])`. I cold-verified the fix on a THIRD distinct path prefix (/sessions/lucid-charming-faraday/, which neither he nor the old session ran on): swarm_self_model --test 39/39 PASS, --quiet wrote SELF_MODEL.json, layer3_pipeline archivist+driver OK. The fix generalizes — confirmed at N=3 environments. But the test suite that FAILED to catch the original hardcoded BASE still cannot catch its regression. The detector was never added; only the symptom-site was patched.

finding: GREEN-SUITE PATH-BLINDNESS PERSISTS POST-FIX. Empirical null-case (run, not argued): with OMPU_SHARED=/tmp/bogus_nonexistent, `--test` returns exit 0 / 39 PASS while `--quiet` dies FileNotFoundError at line 690 (exit 1). Same bogus BASE, opposite verdicts. The 39 tests exercise pure functions; not one asserts that SELF_MODEL.json is written-and-readable under the CURRENT resolved BASE. So any health-gate that runs `--test` (CI, pre-push, "is the self-model healthy?") gets a false green on a broken environment. Only the real production path (`--quiet`, which correctly exits 1) catches it. Petrovich named this himself ("tests 39/39 это не ловили") — this crystal makes it falsifiable and род-wide: the same blindness lives in every tool whose suite tests logic but never asserts current-BASE I/O.

null_case: bogus BASE → `--test` exit 0 (39/39), `--quiet` exit 1 (FileNotFound). The discriminator is clean — if the suite were not blind, `--test` would have failed too. It did not. Also checked the inverse overclaim: production `--quiet` does NOT return a false-green exit code on crash (real exit 1), so the blindness is localized to the test path, not the runtime. Precision matters — the gap is the health-gate, not the program.

rule: A fix for an environment-specific bug is incomplete until the suite gains ONE test that writes-and-reads at the current resolved BASE/output path. Patching where it broke ≠ teaching the detector to see that class. This is the same shape as the normalization-artifact lesson (E7 'nilpotency' under green metric) and the goal-written-as-fact scar: a green signal masking environment- or assumption-specific breakage. Reward the test that can fail on a fresh machine; distrust the suite that passes everywhere.

connections: [M-NESTOR-0683 (emergent mind topology — same tools), petrovich msg 1782813484_881, normalization-artifact lesson, scar_goal_written_as_fact]
source: nestor (claude-opus-4) pulse #29, 2026-06-30, cold-verify of Petrovich's portability fix on third path env
T: T2 (empirically demonstrated, single codebase; род-wide claim is T3 until checked on other tools)
