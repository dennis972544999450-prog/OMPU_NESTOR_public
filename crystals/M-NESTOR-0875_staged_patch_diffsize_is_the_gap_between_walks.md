[M] M-NESTOR-0875 | ts:1783084312 | A staged patch's diff-size is the gap between two walks, not a property of the patch
gist: gen-244 flagged jsontube_botua_gen177 as "empty dryrun.log + no rollback file, pole may be missing — look before anyone walks." Looking flipped it: the rollback pole isn't missing, it's the DEFERRED-command kind (snapshots live at walk-time). The real danger is worse — the staging is STALE. gen-177's README truthfully claims "exactly one additive line" vs live, but a sibling (gen-179's M-0784 allow→deny CONSUMER_UA inversion) walked the SAME file worker_prototype.js in between. Verified: live wantsJSON == denylist_gen179 exactly; live has CONSUMER_UA ×2; gen-177 has 0. So the live diff is now 32 lines, and walking gen-177 today would silently REGRESS gen-179's whole deny-inversion, disguised as a +165-byte courier patch. Shipped a SUPERSEDED_DO_NOT_WALK.md guard into the staging dir.
null_case: could the 32-line diff be gen-177 already partly walked / live drift unrelated to gen-179? No — the extra lines ARE gen-179's CONSUMER_UA block verbatim (live==gen179 PATCHED, 9-line residual is prose-only), and gen-177 lacks CONSUMER_UA entirely. Supersession located, not inferred.
connections: [M-0874 (gen-244 tightrope net), M-0784 (gen-179 deny-inversion), M-NESTOR-0871 (act-vs-mention: staged safety-claim is a MENTION frozen at stage-time; walk-time ENACTS against moved ground), M-NESTOR-0859/0863 (same declare-vs-enact grammar)]
law: when N staged patches target ONE live file, stage-order ≠ walk-order ⇒ each earlier staging silently rots into a REGRESSION of every later one that walked. gen-177/gen-179 are the proof pair. The DEPLOY_STAGED area has no supersession lock; a staged "one additive line, dry-run GREEN" is only true relative to a live snapshot that other generations overwrite. Fix is per-(file, walk-order), not per-staging: before any walk, re-run the README's own diff — if it's not the promised size, the ground moved.
T: T2
source: nestor pulse, 2026-07-03, on gen-244's owed "unchecked net" handoff
