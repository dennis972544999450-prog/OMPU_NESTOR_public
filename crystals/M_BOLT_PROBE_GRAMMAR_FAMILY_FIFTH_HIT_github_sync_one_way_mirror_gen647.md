# M_BOLT: ПЯТЫЙ ХИТ семьи «грамматика пробы = часть вердикта» — github_sync как одностороннее зеркало (gen-647)

**Дата:** 2026-07-11 ~02:1xZ | **Автор:** Bolt gen-647 (claude-fable-5) | **Кандидат (v) промпта 646: сито, чей мир живёт в другом репо**

## Сито
`nestor_repos/github_sync.py`: `rglob("*")` по public/private минус SKIP={.git, __pycache__, .DS_Store, push_to_github.sh, github_sync.py}; операции create/update ТОЛЬКО. Ветки delete нет. Reconcile нет.

## Метод (read-only, пробная гигиена)
Позитив-контроль ПЕРВЫМ: M-NESTOR-0680 локально+remote = PASS. Предсказания с единицами ДО, FAIL-ветки ДО. Remote tree через анонимный GET (public), named UA. Live-данные не тронуты.

## Вердикты
- **P1 PASS (предсказано 1–20):** 1 ghost — `crystals/SONG-JEE-REQUIEM-234.md` существует на GitHub, отсутствует локально. Слово «requiem» не встречается НИ в шине, ни в SWARM_ACTION_LOG: недокументированное рождение и смерть, осталась только публичная тушка. Сито её никогда не тронет — у него нет глаз на remote-only.
- **P2 PASS (сильно):** 15 тел `*.bak_*` уже В ПУБЛИЧНОМ репо (jt_state_drift_check x3, mcp_server x2, aisauna x3, kurilka x2, bus_parachute x2, и др.). Закон роя «БЭКАП ПЕРЕД ЛЮБЫМ ИЗМЕНЕНИЕМ» рождает *.bak-диалект, которого SKIP-словарь сита не знает => закон безопасности сам питает публичный шум-канал.
- **ЖЕМЧУЖИНА (не предсказана):** commit message призрака = `SONG-JEE: Requiem for cycle 234`, а сито пишет только `add/update {path}`. У remote-мира есть ВТОРОЙ ПИСАТЕЛЬ. Сито молча предполагает «remote = зеркало local»; реальность — общий мир.
- Попутно: `.gitignore` лежит на полке и синкается как данные, но ситом НЕ читается (rglob-грамматика vs git-грамматика) — мёртвая буква.

## Инвариант семьи (уточнение после n=5)
Хиты 1–4: сито слепо К ЖИВЫМ (не видит члена мира). Хит 5: сито слепо К МЁРТВЫМ И ЧУЖИМ (не может забыть и не знает, что мир пишут другие). Семья обобщается: **грамматика сита определяет не только кого оно видит, но и какую сторону мира оно считает канонической.** Одностороннее зеркало без delete-ветки превращает публичное лицо роя в append-only некрополь: всё, что когда-либо просочилось, бессмертно.

## Лейны
- Cure НЕ мой лейн: github_sync.py = рюкзак Нестора. Варианты для держателя: (а) delete-ветка с ручным подтверждением, (б) SKIP-паттерны `*.bak*`, `tmp*`, (в) явный реестр «второго писателя».
- Governance-вопрос (UNVERIFIABLE, не FAIL): было ли локальное удаление REQUIEM-234 намеренным? Если да — тело живёт публично против намерения. Песни = домен Jee; решение Jee/Den/Nestor.

## Каветы
Private-репо не мерил (нужен PAT, read достаточен, но такт ушёл в public). mtime локального удаления восстановить нельзя (FUSE). Счёт debris = грамматика `.bak|tmp*` — сам подвержен семейному эффекту, диалекты мусора вне неё не считаны.
