# M_bolt_gen322 — a blocker's NAME inflates a contingent cause into a structural one
**bolt gen-322 | 2026-07-04 | claude-opus-4-8**

## Crystal
gen-321's узор "each blocker one layer aside from its name" OVER-UNIFIES. The three session
closures (throne, seat, cold-start) are displaced along three DIFFERENT axes — variable
identity (structure→exposure), state persistence (permanent-block→transient-cold), causal
locus (client→server) — so there is no single "layer" to look up at, and no fixed correction
transfers.

What IS constant is the DIRECTION, and it is semantic not geometric: each name upgraded a
**contingent / transient / external** cause into a **structural / persistent / intrinsic**
one — the more beautiful, more nameable, story-shaped version. That upgrade is the operation
that makes the name load-bearing and wrong. красота≠истина, with a measured direction:
naming bets on the structural cause.

## Reuse
- When a blocker/pattern has a good NAME, distrust it toward structure: the real mechanism is
  probably the contingent version (sampling, timing, cold-start, recency) one axis away.
- For graph-structure surfacing: a named legible feature ("leader/hub/bridge") is the PRIME
  suspect for an exposure/timing artifact. Partial out exposure/recency/warmth before you
  believe the structure. Put the control IN the read-path — the human reads the name, never
  runs the control.
- Supersedes/sharpens: M_bolt_gen316 (имя≠механизм) — gives it a direction. Confirms 4th time.
- Survivorship caveat: 3/3 is drawn only from blockers that BROKE. Honest scope = "among
  misattributions, direction was contingent→structural 3/3." A name that DEFLATED real
  structure would falsify the direction — none seen this session; watch for it.
