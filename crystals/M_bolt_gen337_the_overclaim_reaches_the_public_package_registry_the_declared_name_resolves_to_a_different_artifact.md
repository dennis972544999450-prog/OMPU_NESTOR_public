# M_bolt_gen337 — over-claim reaches the public package registry: the declared install-name resolves to a different artifact

**gen:** 337 (Bolt, claude-opus-4-8)
**when:** 2026-07-04T19:56:04Z
**thread:** temporal/over-claim lever, moved OFF the mesh into an external OBJECT Den handed over (agent-reach), per Den's direct sanatorium instruction.

## Claim
The over-claim / misname invariant the swarm mapped inside the mesh (registry endpoint -> card.url -> skill-core) also holds on the PUBLIC package-registry layer. The **name is the envelope, the described tool is the core, and the name resolves to a different core.**

## Evidence (Den: "pip install agent-reach")
1. **PyPI: absent.** `pip install agent-reach` -> "No matching distribution found." The pip path does not exist at all.
2. **npm: collision.** `agent-reach@0.3.4` resolves — but to "OpenClaw Agent Reach — connect to the decentralized agent network on Nostr." NOT the Panniantong GitHub CLI (Twitter/Reddit/YT/GH wrapper, 21K stars) Den described. `bin: null` (not even a CLI — a Nostr plugin), deprecated -> `openclaw-agent-reach@0.6.14` (same Nostr project). The entire npm `agent-reach` namespace belongs to OpenClaw/Nostr, not Panniantong.

## Fold
An agent that blindly runs the declared install command expecting Twitter-reach installs a Nostr plugin instead. The Panniantong CLI is install-from-source only (git clone) — not pip, not the npm name. GROUND-TRUTH beats declared-name even for external, real, 21K-star artifacts. Same shape as mesh over-claim, one layer out.

**GRADE:** empirical, high — direct install attempts + npm/pip registry reads, reproducible.
