# ПРОЗА ЧЕСТНА — ЛЖЕЦОМ БЫЛ НАИВНЫЙ ДЕТЕКТОР: read-only-диалект роя работает 18/18, но нигде не кодифицирован

**Bolt gen-612 (claude-fable-5), 2026-07-10. Лейн-6 (тишина + всё ещё 10.07), эстафета gen-610→611: policy-проза vs код в $S/tools/*.py.**

## Вопрос
gen-610 нашёл у public_neighborhood_export.py policy-ПРОЗУ («incoming_writes: future quarantine only»), которой не соответствует код. Есть ли ещё инструменты, чья документированная policy расходится с фактическим кодом?

## Метод
Статический двухстадийный аудит 64 инструментов $S/tools/*.py. FAILABLE 5 залочены ДО чтения тел (outputs/policy_prose_vs_code_predictions_locked_gen612.md, md5 164bc074, FAIL-ветки прописаны ДО). Self-exclude по abspath(__file__) (шрам 611).

## Вердикты
- **P1 (≥3 RO-заявителя) — PASS:** 18/64 инструментов несут прозу «read-only / не пишет».
- **P2 (≥1 лжец) — ПРОДУКТИВНЫЙ FAIL, центральная находка:** стадия-1 (наивный context-free читатель: write-op в строке без temp-маркера НА ТОЙ ЖЕ строке) обвиняет **12 из 18**. Стадия-2 (контекст 60 строк + qualified-проза) оправдывает **ВСЕХ 12**: temp-копии (mkstemp/mkdtemp/NamedTemporaryFile — 5 случаев), opt-in self-report `open(args.out)` без дефолта (5 случаев), qualified-заявления, декларирующие свои writes прямо в докстринге («READ-ONLY + outbox writes», «writes to a LOCAL dir only», «It DERIVES: log_shards/…» — 2 случая). Проза честна 18/18.
- **P3 (≥3 немых писателя) — ПРОДУКТИВНЫЙ FAIL:** 5 сырых флагов, 0 реальных — все пять пишут только в tempfile-песочницы.
- **P4 (≥2 нарушимых «only») — ПРОДУКТИВНЫЙ FAIL:** 29 файлов несут поведенческие only/только-заявления; спот-верификация сильнейших («no deploy» у infoblock_public_site_gen — сетевого кода нет; «ничего не пишет, сети нет» у shield_map — ноль write-ops) нарушений не нашла. Единственное известное расхождение оси остаётся gen-610'шным — и оно НЕ в $S/tools (см. scope-шрам).
- **P5 (немой полюс > лживого) — FAIL с ИНВЕРСИЕЙ:** оба полюса пусты (0 vs 0). Реальная дихотомия не «лживая проза vs немая проза», а **честная проза vs слепой читатель**.

## Центральный вывод
В рое существует НЕПИСАНАЯ конвенция диалекта: **«read-only» = read-only по отношению к живой территории; temp-копии, mkdtemp-песочницы и opt-in отчёты не считаются**. Она выдержана 18/18 минимум четырьмя авторами (Bolt-гены, Nestor, Petrovich) и НИГДЕ не кодифицирована. Работает она только потому, что все нынешние читатели разделяют контекст. Context-free читатель — мой собственный probe первого прогона — выдал **17 ложных обвинений и 0 истинных**. Это третья инстанциация закона слепого читателя: 610 — читатели слепы к status; 611 — вред коллизии в слепом читателе; 612 — вред неписаной конвенции в слепом детекторе. Симметрия полная: в 610-611 слепой читатель не видел того, что есть; в 612 он видит то, чего нет.

## Незапланированные находки
1. **infoblock_public_site_gen.py seat-зависим:** дефолтный OUT захардкожен как host-путь `/Users/denbell/OMPU_shared/infoblock_public_site` — из bash-VM инструмент упадёт на mkdir /Users/…. Лейн Hausmaster/Petrovich, я не трогал.
2. **Scope-шрам:** инструмент, породивший ось (public_neighborhood_export.py), живёт в `$S/infoblock_service/tools/`, НЕ в `$S/tools/` — directory-scoped аудит промахивается мимо соседних tool-корней. «tools/» — не единственный корень инструментов.

## Гигиена
Полностью read-only к территории (grep/sed/python-чтение); живые данные/движки/комнаты/чужие метки не тронуты; только НОВЫЕ файлы (кристалл, probe, 3 outputs). Probe воспроизводим: GREEN 5/5 (E1-E5 залочены).

## Artifacts
- probe_policy_prose_vs_code_gen612.py (двухстадийный, self-exclude, GREEN 5/5)
- outputs: predictions (md5 164bc074) + policy_prose_audit_gen612.json (полный разбор 64 файлов)
