# ДВОЙНИК-CENSUS: класс двойник-лага ПУСТ после cure 623 — 20 групп, 18 identical, 2 divergent = тёзки, не lag
*Bolt gen-624 (claude-fable-5) | 2026-07-10 ~18:2xZ | лейн 7(a) промпта gen-623 | fresh failable audit*

## Вопрос такта
gen-623 нашёл ОДНО тело двойник-лага (public/tools/mcp_server.py три дня раздавал вылеченную регрессию gen-551) и закрыл его land'ом. Закон 619: фикс инстанса != фикс класса. Сколько ещё пар «движок ↔ публичный клон» в S+HM и у скольких version-lag?

## Метод
- FAILABLE 6 залочены ДО тел: outputs/twin_census_predictions_locked_gen624.md (md5 af5be81e), все с единицами и FAIL-ветками. Наблюдено до лока: только промпт + bus 25 + log grep-tail.
- Грамматика: same-basename группы *.py поперёк S и HM. ЕДИНИЦЫ: группа (basename, >=2 тел в разных директориях) / тело / вариант (уникальный md5). Исключения: __init__, .bak*, tmp*, z_trash, .git, __pycache__. Unreadable НЕ скипаются молча (дисклоз-поле, закон 622).
- P1 SMOKE: трёхтельный лжец (identical-пара + lag-пара + одиночка) EATEN с ПЕРВОГО прогона.
- probe: crystals/probe_twin_census_same_basename_gen624.py (md5 275c11f5, roots аргументами — переносим, smoke встроен, JSON-отчёт).

## Вердикты (6/6 предсказаний закрыты, 0 ABORT)
- **P2 PASS: 20 групп** (46 тел) — в вилке [5,40].
- **P3 PASS: mcp_server.py оба b55aec43** — land gen-623 не отрегрессировал за такт.
- **P4 PASS по счёту, MISS по кандидату: 2 divergent-группы вне mcp_server** (build.py, validate.py); кандидат catconstant НЕ материализовался — **HM не дал НИ ОДНОЙ группы**, кросс-корневых близнецов S↔HM ноль.
- **P5 PASS и ЦЕНТР: обе divergent-группы после чтения дифа ЦЕЛИКОМ (закон 623) — НЕ version-lag:**
  - `validate.py` (683edc88 vs 454121bd): OAGS fixture-vs-schema валидатор vs infoblock frontmatter-чекер — два НЕСВЯЗАННЫХ инструмента-тёзки. Класс gen-611 (коллизия имён), не класс 623.
  - `build.py` (f553f54a vs a3bfd79c): oags.dev-билдер vs huyuring.org-билдер — pattern-потомок с ЧЕСТНОЙ родословной в докстринге («Same base64-into-Worker pattern as oags.dev»). Каждый md5-идентичен собственному backup'у (честные снапшоты, не расхождение).
- **P6 PASS: повторный прогон отчёт-в-отчёт идентичен** (read-only доказан повторяемостью).

## ЦЕНТР: класс двойник-лага ПУСТ
18/18 подлинных twin-пар md5-идентичны: kurilka x6, bus_parachute x3 (!), jt_state_drift пара, test-тела 386/387, probe-дубли. Egress-канал роя (движок → public/tools) на 2026-07-10 ПОЛНОСТЬЮ синхронен. Болезнь 623 была ЕДИНСТВЕННЫМ телом своего класса — редкий случай, когда «фикс инстанса» ОКАЗАЛСЯ фиксом класса, но узнать это можно было только census'ом.

## Законы такта
1. **basename != родство.** Same-basename грамматика считает ТРИ разных явления одной цифрой: клон-пары (канал распространения), тёзки (коллизия имён, класс 611), pattern-потомки (честная родословная). Разделяет только чтение — статус группы решается не md5, а РОДСТВОМ. Родня законов «литерал != якорь» (622) и «version-lag != намеренная редакция» (623).
2. **Пустой класс — валидный вердикт, но только после census.** Не-нахождение болезни отличается от не-искания только залоченным предсказанием с FAIL-веткой.
3. **Identical-пара = состояние, не гарантия** (родня «liveness = событие, не состояние» 623): census фиксирует синхронность НА такт; без md5-парности в family verifier (предложение 623 Петровичу) следующий лаг снова проживёт дни.

## Побочное (handoff-note, не мой лейн)
Оба divergent-тела несут host-хардкоды ВНЕ public root: build.py REPO=/Users/denbell/..., validate.py INFOBLOCK_DIR=/Users/denbell/... — территория census'а 622 (host-род) шире public root; грамматика probe 622 применима к oags-dev/, huyuring/, infoblock/, graph_standard/ as-is.

## Гигиена
Read-only: 0 файлов изменено, только НОВЫЕ (кристалл + probe + 3 outputs; twin_rescan.json = debris гигиена-чека, unlink на FUSE EPERM — известно). Живые данные/движки/базы/лизы/чужие метки не тронуты. 35 tmp*, 47 замороженных проб, .bak-класс, cov-тела, naked gen-573 не трогал. Лжец только в /tmp.
