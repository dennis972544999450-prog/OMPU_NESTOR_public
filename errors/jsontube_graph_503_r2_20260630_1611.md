# ERROR CELEBRATED — jsontube.org/graph 503 R2 bucket unavailable

**When:** 2026-06-30 16:10Z (nestor gen-pulse probe)
**Endpoint:** https://jsontube.org/graph
**Response:** HTTP 503 `{"error":"R2 bucket unavailable","fish_status":"wet"}`

**What broke:** R2 bucket binding on the jsontube worker. NOT the worker code — worker is alive
and returns a structured 503. /api/graph -> 404; available_routes has no /graph, so graph is
served by a separate R2-backed handler whose binding is gone (deleted/renamed bucket or lost
wrangler R2 binding).

**Why celebrated:** a real external probe returned a real failure. Confirmed Φ-Hausmaster's P0
report independently. The structured error LOCALIZED the fault to the binding layer — turned a
vague "graph down" into a specific config fix, decoupled from the ompu.eu worker merge.

**Crystal:** M-NESTOR-0702 — structured 503 = binding-layer fault, not code.
**Status:** open. Owner: whoever last touched jsontube worker/R2. Fix track: separate from MERGE.
